# FloatPocket 悬浮工具 AI 开发指令

# \#\# 项目概述

开发一款Windows轻量化悬浮工具FloatPocket，核心实现系统托盘常驻、悬浮球交互、剪贴板图片OCR/翻译、单层分类快捷应用启动、轻量化计划表，本地存储无云端交互，适配自用场景。

# \#\# 开发规范

- 采用模块化开发，拆分主入口、托盘、悬浮球、OCR翻译、快捷应用、计划表、配置7大模块，低耦合。

- 技术栈固定为Python 3\.9\+、PySide6，用uv管理依赖，适配Windows 10/11 64位系统。

- 优先实现核心功能，再补充优化，所有配置、数据本地存储，不依赖云端。

- 代码添加简洁注释，明确关键逻辑，便于调试和迭代。

# \#\# 代码风格

- 命名规范：变量、函数、类名采用小写下划线命名，贴合功能，不使用模糊命名。

- 代码简洁，避免冗余逻辑，注重可读性，适配AI读取和调试。

- 格式统一，缩进一致，避免过长代码行，保持代码整洁。

# \#\# 测试要求

- 功能测试：覆盖托盘、悬浮球、OCR/翻译、快捷应用、计划表所有核心操作。

- 稳定性测试：后台常驻无闪退，数据不丢失，高频操作无卡顿。

- 兼容性测试：适配Windows 10/11，打包为单EXE，免安装、无依赖冲突。

# \#\# 注意事项

- 依赖管理：用uv管理依赖，通过pyproject\.toml配置，避免版本冲突。

- API调用：妥善配置OCR/翻译API密钥，捕获网络、密钥异常并给出提示。

- 数据安全：所有数据本地存储，无云端上传，保障隐私和数据安全。

> （注：文档部分内容可能由 AI 生成）
